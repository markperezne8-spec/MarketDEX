# Workspace Tab Access CI Review

Review focus: `tests/test_viewport_fit_feature.py` must assert tabs enabled before selection and no forced redirect after selection clears; `.github/workflows/ci.yml` Desktop Build must run the viewport suite only once.
