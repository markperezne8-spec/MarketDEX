# Release Delivery Boundary

The current milestone is operator-facing Windows RC delivery, not installer engineering.

Completion requires a manually invokable Windows build that verifies the executable, stages a package with operator guidance, publishes the package as an artifact, preserves source authority, and supports operator clean-launch verification.

Installer registration, automatic updates, code signing, permanent public release assets, marketplace connectivity, and cloud persistence remain outside this milestone.
