# MarketDEX Startup

Startup order:

1. launcher.py
2. app.core.bootstrap.bootstrap()
3. lifecycle.startup()
4. create application
5. application.run()
6. lifecycle.shutdown()

Repository note:
The project now uses `app/` as the root package instead of `src/`.
