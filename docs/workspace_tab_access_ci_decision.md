# Workspace Tab Access CI Decision

Keep navigation regression coverage in `tests/test_viewport_fit_feature.py`, the existing authoritative viewport suite, rather than maintaining a second source-text inspection test.
