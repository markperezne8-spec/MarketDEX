# RC Delivery Manifest

Operator package name: `MarketDEX-Windows-RC-Operator-Package`

Package contents:

- `MarketDEX.exe` — verified standalone Windows application.
- `README.txt` — concise extraction, launch, repository-separation, and offline-first guidance.

The package does not contain source code, the Git repository, marketplace credentials, cloud configuration, or an installer.
