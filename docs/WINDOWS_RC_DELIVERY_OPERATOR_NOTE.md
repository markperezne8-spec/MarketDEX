# Windows RC Delivery Operator Note

📦 Build products are delivered separately from source code.

⬇️ Pull only when `main` advances and the operator is explicitly shown `🟢 PULL NOW`.

🪟 Download the Windows RC operator package from its successful delivery workflow run, extract it outside the repository, and launch `MarketDEX.exe`.

🔴 Never merge `MarketDEX.exe` into the Git repository.
