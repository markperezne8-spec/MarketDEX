# RC Delivery Review Checklist

Before merge:

- Confirm the delivery workflow is manual.
- Confirm the build uses the authoritative `launcher.py` packaging specification.
- Confirm executable verification occurs before artifact publication.
- Confirm package guidance tells the operator to extract outside the repository.
- Confirm no generated `.exe` is committed.
- Confirm installer and automatic-update scope remain excluded.
