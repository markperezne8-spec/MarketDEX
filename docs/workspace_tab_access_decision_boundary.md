# Workspace Tab Access Decision Boundary

Top-level workspace navigation is operator authority and is never conditional on row selection.

Asset-specific workflow execution is context-dependent and may require inventory selection.

MarketDEX therefore gates action buttons, not workspace visibility.
