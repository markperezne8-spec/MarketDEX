# Marketplace Listing Preparation Summary Contract

Prepared state summary must contain `LISTING PACKAGE READY` and `OFFLINE PREPARATION ONLY`.

Blocked state summary must contain `PACKAGE BLOCKED`.

The package must visibly label MARKETPLACE, TITLE, QUANTITY, TARGET PRICE, and ASSET ID.
