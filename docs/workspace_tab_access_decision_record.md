# Workspace Tab Access Decision Record

Decision: remove selection-driven top-level tab locking.

Reason: Pricing and Listing Workflow are operator workspaces, while inventory selection is transient workflow context.

Consequence: tabs remain openable; context-dependent Continue actions remain disabled until an asset is selected.
