# Workspace Tab Access Operator Authority

The operator owns workspace navigation. MarketDEX may explain that an asset is required for a workflow action, but it must not hide, disable, or forcibly close a top-level workspace because no inventory row is selected.
