# Workspace Tab Access CI Completion

Complete when the follow-up pull request passes MarketDEX CI and is merged to `main`. Operator then pulls `main` and verifies both tabs in the maximized Windows desktop app.
