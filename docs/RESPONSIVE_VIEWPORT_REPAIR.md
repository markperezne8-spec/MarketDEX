# Responsive Viewport Repair

Mission Control no longer caps the primary workspace at a fixed 820 pixel width.

The workspace expands with the application window. Inventory table height is bounded so lower decision workflow content remains reachable through the existing resizable scroll area.

The launcher opens at a taskbar-aware size within the primary screen's available geometry and retains normal Windows maximize, restore, and resize behavior.
