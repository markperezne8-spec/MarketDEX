# M1.10 Health Composition-Root Readiness Planning

**Status:** Completed through M1.10E
**Canonical branch:** `main`  
**Source of truth:** GitHub repository `markperezne8-spec/MarketDEX`

## Milestone intent

M1.10 prepares deterministic, explicit Health composition-root registration contracts over the completed M1.9 application-boundary API without authorizing startup execution.

## Approved authority

- Preserve the public contracts completed through M1.9.
- Reuse `HealthApplicationRequest`, `HealthApplicationBoundary`, application payload, and application diagnostic-line projections.
- Permit only injected, deterministic, offline-first, side-effect-free registration contracts.
- Preserve boundary identity, runtime identity, and provider order exactly.
- Require focused contract tests before any startup exposure.

## Guardrails

- Registration never executes Health checks by default.
- No automatic startup execution, live checks, polling, persistence, network access, scheduler behavior, automation, notifications, UI, logging side effects, or business-state mutation.
- Any startup invocation requires a separately scoped issue after registration contracts are locked.
- Visible Health Monitor work remains separately scoped and requires Mark visual acceptance.
- Continue issue → branch → draft PR → CI → ready → squash-merge.

## Suggested sequence

1. M1.10A — immutable composition-root registration contract.
2. M1.10B — deterministic registry adapter.
3. M1.10C — registered application payload projection.
4. M1.10D — registered diagnostic-line projection.
5. M1.10E — public API contract and checkpoint synchronization.

Each slice must remain tightly scoped, non-visual, and independently mergeable.

## Visual and tooling status

- Visual check: Not required.
- Specialist tooling: Not used.
- PC action: None needed during remote GitHub-first work.

## M1.10 completion

- M1.10A added the immutable `HealthRootRegistration` contract.
- M1.10B added the deterministic `RegisteredHealthRoot` registry adapter.
- M1.10C added the registered-root application payload projection.
- M1.10D added the registered-root diagnostic-line projection.
- M1.10E synchronized the approved root contracts through the `app.engines.health` public API.
- All M1.10 contracts remain deterministic, offline-first, side-effect-free, and without startup execution, polling, persistence, networking, scheduling, UI, logging side effects, or business-state mutation.
- Public API synchronization is limited to the five approved root symbols; no snapshot contract is exported.
