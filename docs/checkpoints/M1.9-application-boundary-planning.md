# M1.9 Health Monitor Application-Boundary Readiness Planning

**Status:** Planning active  
**Canonical branch:** `main`  
**Source of truth:** GitHub repository `markperezne8-spec/MarketDEX`

## Milestone intent

M1.9 prepares deterministic Health Monitor application-boundary adapters over the completed M1.8 runtime-composition contracts without authorizing startup execution or any side effect.

## Approved authority

- Preserve the public contracts completed in M1.6, M1.7, and M1.8.
- Reuse `HealthRuntimeComposition` and its approved report, line, and summary projections.
- Permit only injected, deterministic, offline-first, side-effect-free adapters.
- Require focused contract tests before any composition-root exposure.
- Preserve provider ordering and runtime identity exactly.

## Guardrails

- No adapter executes during startup by default.
- No live checks, polling, persistence, network access, scheduler behavior, automation, notifications, UI, logging side effects, or business-state mutation.
- Composition-root exposure requires a separately scoped issue after adapter contracts are locked.
- Visible Health Monitor work requires a separately scoped visible build and Mark visual acceptance.
- Continue issue → branch → draft PR → CI → ready → squash-merge.

## Suggested sequence

1. M1.9A — immutable application-boundary request or input contract.
2. M1.9B — deterministic adapter over `HealthRuntimeComposition`.
3. M1.9C — application-boundary payload projection.
4. M1.9D — application-boundary diagnostic-line projection.
5. M1.9E — public API contract and checkpoint synchronization.

Each slice must remain tightly scoped, non-visual, and independently mergeable.

## Visual and tooling status

- Visual check: Not required.
- Specialist tooling: Not used.
- PC action: None needed during remote GitHub-first work.
