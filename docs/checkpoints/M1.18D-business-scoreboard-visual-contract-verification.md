# M1.18D Business Scoreboard Visual and Contract Verification

**Status:** Verification checkpoint complete
**Canonical branch:** `main`
**Source of truth:** GitHub repository `markperezne8-spec/MarketDEX`
**Issue:** #530
**Parent checkpoint:** `docs/checkpoints/M1.18-mission-control-business-scoreboard-planning.md`

## Verification intent

M1.18D records accepted visual evidence and contract verification for the Mission Control `Business Scoreboard` surface.

This checkpoint introduces no runtime behavior, UI layout change, view-model change, test behavior change, persistence change, dependency change, route change, provider integration, automation, alert, notification, command path, manual KPI input, or business-data mutation. It preserves the verified result of prior M1.18 implementation slices.

## Accepted implementation evidence

| Slice | PR | Merge commit | Verified result |
|---|---:|---|---|
| M1.18 planning | #523 | `be0d17c` | Planned the contract-first `Business Scoreboard` milestone. |
| M1.18A | #525 | `45e772a` | Added the immutable non-visual Business Scoreboard view-model contract. |
| M1.18B | #527 | `9bee1c1` | Added the first visible read-only `Business Scoreboard` shell. |
| M1.18C | #529 | `b9a0542` | Hardened deterministic display states, labels, ordering, period preservation, and inline error-safe evidence. |

CI passed for the visible-shell and display-state implementation slices before merge:

- M1.18B PR #527 passed CI run #782 before squash merge.
- M1.18C PR #529 passed CI run #784 before squash merge.

## Visual acceptance evidence

Mark supplied and accepted visual evidence after the M1.18B run.

The accepted visual evidence confirms:

- `Business Scoreboard` appears in Mission Control after `Opportunity + Risk`;
- `Business Scoreboard` appears before `Dashboard Grid`;
- the section title and subtitle are visible as `Business Scoreboard` and `Read-only period performance`;
- `Selected period: 90 DAYS` is visible;
- the default missing-evidence state renders honest `Unavailable` output;
- `Money` and `Throughput` remain distinct visible groups;
- the Money group shows unavailable Seller Net Proceeds, Net Business Gain, and Capital Recycled This Period rows;
- the Throughput group shows unavailable Orders Completed, Items Sold, and Median Days to Sell rows;
- existing accepted Mission Control surfaces remain present.

M1.18D itself does not require a new visual check because it records visual acceptance already supplied by Mark and contract verification already locked by focused tests.

## Contract verification

The accepted M1.18 surface preserves the planned contract:

- local-only prepared or injected evidence only;
- missing evidence renders `Unavailable` instead of invented sales, gain, capital-recycled, order, item, or sell-speed values;
- deterministic group order is preserved as `Money` before `Throughput`;
- deterministic Money metric order is preserved as Seller Net Proceeds, Net Business Gain, and Capital Recycled This Period;
- deterministic Throughput metric order is preserved as Orders Completed, Items Sold, and Median Days to Sell;
- display states are locked as `Ready`, `Unavailable`, `Partial`, `Not applicable`, and `Error-safe`;
- error-safe text renders inline and non-modal;
- selected period labels are preserved from the immutable view model;
- source authority and calculation authority text remain explicit;
- status display remains plain-language and read-only;
- the surface renders from injected or prepared view-model evidence and does not fetch or calculate values itself;
- no fake period-performance values are displayed.

## Read-only and injected-only verification

The accepted M1.18 surface remains read-only and injected-only:

- `BusinessScoreboardPanel` renders from an injected `BusinessScoreboardViewModel` or the default unavailable view model;
- focused panel tests verify that injected models are rendered without falling back to an internal builder;
- focused panel tests verify no `QPushButton` action controls are present inside the `Business Scoreboard` surface;
- focused panel tests verify inline error-safe text without dialogs;
- focused panel tests verify placement before `Dashboard Grid`;
- M1.18C contract tests verify state matrix and metric ordering without introducing formulas or real values.

## Guardrails preserved

M1.18D verifies that the M1.18B-M1.18C accepted surface does not authorize or introduce:

- manual KPI entry;
- action controls;
- task execution;
- popup or dialog behavior from the Business Scoreboard surface;
- Buy or Sell commands;
- payout, settlement, grade, list, purchase, ship, or reconciliation commands;
- formula invention;
- calculation engine behavior;
- fake sales values;
- fake gain values;
- fake capital-recycled values;
- fake order, item, or sell-speed values;
- marketplace or live-pricing integration;
- financial-provider integration;
- polling;
- background workers;
- networking;
- persistence changes;
- database migration;
- alerts;
- notifications;
- automation;
- route rewrites;
- new workspaces;
- dependency changes;
- business-state mutation.

## Next safe movement

The next M1.18 slice may synchronize checkpoint authority after M1.18D merges.

Future real `Business Scoreboard` values still require a separately approved local evidence and calculation-meaning contract. Future Mission Control areas such as listing readiness, inventory age, storage review, audit coverage, charts, alerts, heat maps, or market attention trend remain contract-first and independently scoped.
