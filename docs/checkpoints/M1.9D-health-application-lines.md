# M1.9D — Health application diagnostic lines

## Status
Draft PR scope for deterministic Health Monitor application-boundary diagnostic lines.

## Scope
- Adds `health_application_lines` over the approved `HealthApplicationBoundary` contract.
- Emits boundary identity first, then the approved runtime health diagnostic lines.
- Preserves runtime identity and provider ordering exactly.
- Adds focused validation tests.

## Non-goals
No composition-root exposure, startup wiring, live checks, polling, persistence, network behavior, scheduler behavior, automation, notifications, UI, logging side effects, or business-state mutation.

## Verification
Focused tests and required CI. Visual check not required.
