# Release Candidate Next Boundary

Next: execute the Windows release-candidate verification checkpoint against the packaged MarketDEX executable and preserved operator runtime data. Repair only defects found by that checkpoint. When the five permanent CI gates are green and the Windows checkpoint passes, mark the exact commit as the verified release-candidate checkpoint.
