# Release Candidate Operator Note

Operator action is intentionally deferred until this checkpoint-contract change is merged and the exact candidate commit is available on `main`.

Do not use an older executable to certify the new checkpoint. The Windows verification must be performed against a package produced from the exact candidate commit whose five permanent CI gates are green.
