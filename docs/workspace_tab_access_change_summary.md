# Workspace Tab Access Change Summary

Changed `ui/viewport_fit_feature.py` so inventory selection no longer disables Pricing or Listing Workflow tabs and no longer forces the active workspace back to Inventory.

Added focused regression coverage and a dedicated PR gate.
