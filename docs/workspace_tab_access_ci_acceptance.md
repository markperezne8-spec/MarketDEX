# Workspace Tab Access CI Acceptance

Desktop Build must pass `tests/test_viewport_fit_feature.py`, including direct tab navigation with no selected asset and persistence of the active Listing Workflow tab when selection is cleared.
