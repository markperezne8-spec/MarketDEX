# Marketplace Listing Preparation Title Policy

The first offline preparation title is deterministic: `{asset_name} | {asset_type}`.

This build does not claim marketplace title optimization. The deterministic title makes package output visible, testable, and traceable before a future listing review build.
