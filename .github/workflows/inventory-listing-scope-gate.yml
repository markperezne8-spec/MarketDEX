name: Inventory Listing Scope Gate
on: [pull_request, workflow_dispatch]
jobs:
  listing-scope:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pytest
      - run: pytest -q tests/test_listing_workspace_scope_contract.py
