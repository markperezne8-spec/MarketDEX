name: RC Delivery Manifest Gate

on:
  pull_request:
    paths:
      - 'docs/RC_DELIVERY_MANIFEST.md'
      - 'tests/test_rc_delivery_manifest_contract.py'
      - '.github/workflows/rc-delivery-manifest-gate.yml'

jobs:
  rc-delivery-manifest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pytest -q tests/test_rc_delivery_manifest_contract.py
